#!/command/with-contenv bashio
# shellcheck shell=bash
# ==============================================================================
# Start UACMEs service
# ==============================================================================

REFRESH=$(bashio::config 'refresh')

exec snooze ${REFRESH} -t /var/cache/snooze/renew -- /etc/s6-overlay/scripts/renew-cert
